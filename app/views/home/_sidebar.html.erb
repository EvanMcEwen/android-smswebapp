<div class="container-fluid">
  <div class="row-fluid">
        <div class="span3">
        <h2>Conversations</h2>
        <% if !@conversations.blank? %>
            <ul class="nav nav-tabs nav-stacked nav-conversation">
            <li class="active"><a href="#">Active Class</a></li>
          <% for item in @conversations %>
            <li><a href="#"><%= item %></a></li>
          <% end %>
            </ul>
        <% else %>
        <% end %>
        </div>